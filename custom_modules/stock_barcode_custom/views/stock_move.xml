<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_stock_move_operations_custom" model="ir.ui.view">
            <field name="name">stock.move.operations.form.custom</field>
            <field name="model">stock.move</field>
            <field name="inherit_id" ref="stock.view_stock_move_operations"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='display_assign_serial']" position="after">
                    <field name="x_packaging" invisible="1" />
                </xpath>

                <xpath expr="//field[@name='move_line_ids']" position="replace">
                    <field name="move_line_ids" attrs="{'readonly': ['|', ('state', '=', 'cancel'), '&amp;', ('state', '=', 'done'), ('is_locked', '=', True)]}" context="{'tree_view_ref': 'stock.view_stock_move_line_operation_tree', 'default_product_uom_id': product_uom, 'default_picking_id': picking_id, 'default_move_id': id, 'default_product_id': product_id, 'default_location_id': location_id, 'default_location_dest_id': location_dest_id, 'default_company_id': company_id, 'default_package_id': x_packaging, 'default_result_package_id': x_packaging }"/>
                </xpath>
            </field>

        </record>

    </data>
</odoo>